{% set blockClass = 'iconedText' %}
{% set blockTag = link ? 'a' : 'button' %}

<{{ blockTag }} {% if link %}href="{{ link }}"{% endif %}
    class="{{ parentClass ? bem(parentClass, blockClass) }} {{ bem(blockClass, [icon]) }}">{{ text }}</{{ blockTag }}>
